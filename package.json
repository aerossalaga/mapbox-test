{
  "name": "mapbox-test",
  "version": "1.0.0",
  "description": "This project displays a map of cities in Kansas using Mapbox GL. It geocodes city names from a JSON file, adds markers to the map, and provides interactive features.",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server --port=8080 --open=index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
